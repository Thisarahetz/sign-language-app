import CustomModal from "@components/Common/Modals";
import React from "react";

export default function DriverDocumentsModal() {
  return (
    <CustomModal open={true} close={() => {}} modalTitle="t" opened={false}>
      <div></div>
    </CustomModal>
  );
}
